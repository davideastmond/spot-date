export const AlgoliaController = {
  // This is in charge of creating and updating an entry in the Angolia index
  signUpReconcile: async () => {
    // First check if it exists. If it doesn't, create it.
  },
  createIndexIfNotExists: async (indexName: string) => {},
};
